# Repository Instructions

Use the canonical skill at `skills/engineering-agent-hierarchy/SKILL.md`.

Provider-specific wrappers must remain thin. Do not fork the engineering workflow into separate Claude and Codex copies. Preserve `references/development-execution-contract.md` as the authoritative execution contract unless explicitly asked to revise it. Validate structural changes with `python scripts/validate.py`.
